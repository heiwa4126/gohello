package calc

import (
	"testing"
)

func TestAddSub(t *testing.T) {
	var r int

	r = Add(1, 2)
	if r != 3 {
		t.Errorf("Add() failed. expect:%d, actual:%d", 3, r)
	}

	r = Sub(1, 2)
	if r != -1 {
		t.Errorf("Add() failed. expect:%d, actual:%d", -1, r)
	}
}
